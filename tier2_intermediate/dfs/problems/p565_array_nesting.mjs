/**
 * P565: Array Nesting [PREMIUM] (Medium)
 * https://leetcode.com/problems/array-nesting/
 * Topics: Array, Depth-First Search
 *
 * You are given an integer array nums of length n where nums is a permutation of [0, n-1].
 * Return the longest length of a set s[k] = {nums[k], nums[nums[k]], ...}.
 *
 * Example 1:
 *     Input: nums = [5,4,0,3,1,6,2]
 *     Output: 4
 *
 * Example 2:
 *     Input: nums = [0,1,2]
 *     Output: 1
 *
 * Constraints:
 *     - 1 <= nums.length <= 10^5
 *     - 0 <= nums[i] < nums.length
 *
 * Template (javascript):
 *     function arrayNesting(nums: number[]): number
 */

import { readLine, readInts, readInt, writeInt, writeInts, writeString, writeBool } from '../../wasm_libs/js/io.mjs';

function solve() {
  throw new Error("NotImplementedError");
}

solve();
